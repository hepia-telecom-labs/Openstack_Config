global_defs {
  router_id controller-1
}
vrrp_instance 42 {
  virtual_router_id 42
  # for electing MASTER, highest priority wins.
  priority  100
  state     SLAVE

  interface eth0

  virtual_ipaddress {
        10.2.4.100
  }
}
